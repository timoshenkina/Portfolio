# 🌐 Как работает Мультиязычность (Internationalization)

Ты спросила, как это работает логически. Давай разберем "на пальцах".

Для компьютера сайт — это набор текстов. Чтобы сайт "заговорил" на разных языках, мы не копируем сайт 3 раза (это сложно поддерживать). Вместо этого мы используем **"Словари"**.

## 1. Логика "Словарей" (JSON)

Мы создадим папку `messages`, и в ней будет 3 файла (похожих на таблички):

**📄 en.json (Английский)**
```json
{
  "Hero": {
    "title": "Graphic & UX/UI Designer",
    "cta": "Email Me"
  }
}
```

**📄 uk.json (Украинский)**
```json
{
  "Hero": {
    "title": "Графічний та UX/UI Дизайнер",
    "cta": "Написати мені"
  }
}
```

**📄 ru.json (Русский)**
```json
{
  "Hero": {
    "title": "Графический и UX/UI Дизайнер",
    "cta": "Написать мне"
  }
}
```

## 2. Как это выглядит в коде?

Вместо того чтобы писать текст прямо в коде:
```tsx
<h1>Graphic & UX/UI Designer</h1> ❌ Было
```

Мы пишем "ключ" из словаря:
```tsx
<h1>{t('Hero.title')}</h1> ✅ Станет
```

Библиотека `next-intl` (это и есть тот самый "плагин") сама посмотрит, какой язык выбрал пользователь, и подставит нужный текст.

## 3. План реализации

Вот шаги, которые я сделаю, чтобы подключить эту систему:

1.  **Установка библиотеки:** `npm install next-intl`
2.  **Создание словарей:** Создам файлы `en.json`, `uk.json`, `ru.json` и перенесу туда все тексты с главной страницы.
3.  **Настройка "Маршрутизатора" (Middleware):**
    *   Если пользователь заходит на `site.com` -> открывается язык по умолчанию (en).
    *   Если на `site.com/uk` -> открывается украинский.
    *   Если на `site.com/ru` -> открывается русский.
4.  **Создание переключателя (Switcher):** Кнопки 🇺🇸 🇺🇦 🇷🇺 в меню, которые меняют ссылку.

## Почему это лучшее решение?
*   **SEO:** Google отлично понимает такую структуру (`/en`, `/uk`).
*   **Удобство:** Чтобы исправить ошибку в тексте, не нужно лезть в сложный код, нужно просто поправить файл `.json`.
*   **Скорость:** Это работает очень быстро и не нагружает сайт.

---

Если ты согласна с этим планом, я начну установку `next-intl` и настройку структуры!
